# Schema Drift Detector CLI

A command-line tool for detecting schema drift.

## Installation

```bash
pip install -e .
```

## Usage

```bash
python -m schema_drift_cli --help
```

### Commands

- `version` - Show the CLI version